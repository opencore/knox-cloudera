---
PROJECT_NAME: knox
PROJECT_VERSION: 1.2.0
PARCEL_VERSION: 1

DOWNLOAD_FILE_NAME: knox-{{ PROJECT_VERSION }}.zip
DOWNLOAD_PATH: "{{ PROJECT_VERSION }}/{{ DOWNLOAD_FILE_NAME }}"

EXTRACTED_DIR_NAME: knox-{{ PROJECT_VERSION }}
